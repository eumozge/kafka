[project]
name = "study"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
    "confluent-kafka[avro]>=2.11.1",
    "orjson>=3.11.3",
    "pydantic-settings>=2.10.1",
]

[dependency-groups]
dev = ["pre-commit"]
lint = [
    "mypy",
    "ruff",
]

[tool.uv.workspace]
members = []
